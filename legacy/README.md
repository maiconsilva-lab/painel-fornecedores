# Protótipos desativados

`recepcao-age.jsx.disabled` foi preservado apenas como referência histórica. Ele não é uma rota válida do Next.js e não participa do build nem do deploy. A funcionalidade ativa de pendências fiscais está em `app/pendencias/page.js` e usa rotas autenticadas no servidor.
